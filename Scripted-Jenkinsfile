node {
    def mvnPath
    
    stage('git clone process') {
        git 'https://github.com/akashwaghmare3214/maven-web-app'
    }
    
    stage('Maven build'){
        def mvnHome = tool name:'maven', type:"maven";
        mvnPath = "${mvnHome}/bin/mvn";
        sh "${mvnPath} clean package"
    }    
}
